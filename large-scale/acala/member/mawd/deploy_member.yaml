apiVersion: apps/v1
kind: Deployment
metadata:
  name: acala-member-mawd
  namespace: monitoring
spec:
  replicas: 1
  selector:
    matchLabels:
      app: acala-member-mawd
  template:
    metadata:
      name: acala-member-mawd
      labels:
        app: acala-member-mawd
    spec:
      nodeSelector:
        node-role.kubernetes.io/master: ""
      tolerations:
      - key: "node-role.kubernetes.io/master"
        operator: "Equal"
        effect: "NoSchedule"
      containers:
      - name: acala-member-mawd
        image: chuangtw/acalamembermawd:latest
        imagePullPolicy: Always
        volumeMounts:
        - mountPath: /root/.kube/config
          name: kube-config
        resources:
          requests:
            cpu: 100m
            memory: 200Mi
        ports:
        - name: acala-member-mawd
          containerPort: 54088
      volumes:
      - name: kube-config
        hostPath:
          path: /root/.kube/config
          type: File
---
apiVersion: v1
kind: Service
metadata:
  name: acala-member-mawd
  namespace: monitoring
  labels:
    name: acala-member-mawd
spec:
  selector:
    app: acala-member-mawd
  type: NodePort
  ports:
  - name: acala-member-mawd
    protocol: TCP
    port: 54088
    nodePort: 31580